from typing import Optional

from app.models.user import User


def authenticate_user(username: str, password: str) -> Optional[User]:
    """Placeholder authentication function."""
    # Replace with real lookup and verification
    return None

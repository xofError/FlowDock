services:
  # --- GATEWAY (NGINX) ---
  nginx:
    image: nginx:latest
    container_name: flowdock_gateway
    restart: always
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
      - flowdock_public
    depends_on:
      - frontend
      - auth_service
      - media_service

  # --- DATABASES ---
  postgres:
    image: postgres:16
    container_name: flowdock_postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: FlowDock
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - flowdock_internal
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7
    container_name: flowdock_redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redisdata:/data
    networks:
      - flowdock_internal
      - flowdock_public
    # 👇 ADD THIS BLOCK BACK 👇
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  mongodb:
    image: mongo:7
    container_name: flowdock_mongodb
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - mongodata:/data/db
    networks:
      - flowdock_internal
      - flowdock_public
    command: mongod --noauth
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 60s

  rabbitmq:
    image: rabbitmq:4.0-management
    container_name: flowdock_rabbitmq
    restart: always
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    ports:
      - "5672:5672"
      - "15672:15672"
    volumes:
      - rabbitmqdata:/var/lib/rabbitmq
    networks:
      - flowdock_internal
      - flowdock_public
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 40s

  pgadmin:
    image: dpage/pgadmin4
    container_name: flowdock_pgadmin
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@pgadmin.com
      PGADMIN_DEFAULT_PASSWORD: admin
    ports:
      - "5050:80"
    networks:
      - flowdock_public
      - flowdock_internal
    depends_on:
      postgres:
        condition: service_healthy

  # --- APP SERVICES ---
  frontend:
    build:
      context: ./frontend/frontend
      dockerfile: ../Dockerfile
    container_name: flowdock_frontend
    restart: always
    environment:
      # Points to the Nginx routes we defined above
      - VITE_AUTH_API_URL=/api
      - VITE_MEDIA_API_URL=/media
    networks:
      - flowdock_public
    depends_on:
      - auth_service

  auth_service:
    build:
      context: ./backend/auth_service
      dockerfile: Dockerfile
    container_name: flowdock_auth_service
    restart: always
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: FlowDock
      REDIS_HOST: redis
      REDIS_PORT: 6379
      JWT_SECRET: "secret"
      JWT_ALGORITHM: "HS256"
      SMTP_HOST: "smtp.gmail.com"
      SMTP_PORT: "587"
      SMTP_USER: "flowdockproduction@gmail.com"
      SMTP_PASSWORD: "hmlz qbkb rbbb npja"
      SMTP_FROM_EMAIL: "flowdockproduction@gmail.com"
      SMTP_FROM_NAME: "FlowDock"
      # Localhost URLs for testing
      BACKEND_URL: "http://localhost"
      FRONTEND_URL: "http://localhost"
    ports:
      - "8000:8000"
    networks:
      - flowdock_public
      - flowdock_internal
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  media_service:
    build:
      context: ./backend/media_service
      dockerfile: Dockerfile
    container_name: flowdock_media_service
    restart: always
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: FlowDock
      MONGO_URL: "mongodb://mongodb:27017/flowdock"
      MONGO_DB_NAME: "flowdock"
      RABBITMQ_URL: "amqp://guest:guest@rabbitmq:5672/"
      RABBITMQ_QUEUE: "file_events"
      ENCRYPTION_MASTER_KEY: "0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef"
      LOG_LEVEL: "INFO"
      # Auth Service URL for user lookups
      AUTH_SERVICE_URL: "http://auth_service:8000"
    ports:
      - "8001:8000"
    networks:
      - flowdock_public
      - flowdock_internal
    depends_on:
      mongodb:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      postgres:
        condition: service_healthy

  # --- MONITORING ---
  prometheus:
    image: prom/prometheus:latest
    container_name: flowdock_prometheus
    restart: always
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    ports:
      - "9090:9090"
    networks:
      - flowdock_public
      - flowdock_internal

  # grafana:
  #   image: grafana/grafana:latest
  #   container_name: flowdock_grafana
  #   restart: always
  #   environment:
  #     GF_SECURITY_ADMIN_PASSWORD: admin
  #     GF_SERVER_ROOT_URL: http://localhost/grafana
  #     GF_SERVER_SERVE_FROM_SUB_PATH: "true"
  #   ports:
  #     - "3000:3000"
  #   volumes:
  #     - grafana_data:/var/lib/grafana
  #   networks:
  #     - flowdock_public
  #     - flowdock_internal

volumes:
  pgdata:
  redisdata:
  mongodata:
  rabbitmqdata:
  prometheus_data:
  grafana_data:
  pipcache:

networks:
  flowdock_public:
    driver: bridge
  flowdock_internal:
    driver: bridge
    internal: true